import { AzeDateTimePipe } from './aze-date-time.pipe';

describe('AzeDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new AzeDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
