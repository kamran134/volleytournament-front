import { AzeFullDatePipe } from './aze-full-date.pipe';

describe('AzeFullDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AzeFullDatePipe();
    expect(pipe).toBeTruthy();
  });
});
